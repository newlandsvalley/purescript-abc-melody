{
  "private": true,
  "scripts": {
    "build": "spago build",
    "test": "spago -x ./test.dhall test",
    "player": "spago -x ./player.dhall bundle-app --main Player.Main --to player/dist/app.js"
  },
  "devDependencies": {
    "spago": "^0.19.8",
    "purescript": "^0.15.0",
    "purescript-psa": "^0.5.1"
  }
}
